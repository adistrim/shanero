export const PATHS = {
    app: {
        auth: {
            login: '/auth/login',
            signup: '/auth/signup',
            resetPassword: '/auth/reset-password',
        },
    }
} as const;
